<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.game">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Game">
        <activity
            android:name=".score"
            android:label="@string/title_activity_score"
            android:theme="@style/Theme.Game.NoActionBar"></activity>
        <activity
            android:name=".QuizActivity"
            android:label="@string/title_activity_quiz"
            android:theme="@style/Theme.Game.NoActionBar" />
        <activity
            android:name=".startcategory"
            android:label="@string/title_activity_startcategory"
            android:theme="@style/Theme.Game.NoActionBar" />
        <activity
            android:name=".addquastion"
            android:label="@string/title_activity_addquastion"
            android:theme="@style/Theme.Game.NoActionBar" />
        <activity
            android:name=".addcategory"
            android:label="@string/title_activity_addcategory"
            android:theme="@style/Theme.Game.NoActionBar" />
        <activity
            android:name=".adminactivity"
            android:label="@string/title_activity_adminactivity"
            android:theme="@style/Theme.Game.NoActionBar" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.Game.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegisterActivity" />
        <activity android:name=".DashboardActivity" />
    </application>

</manifest>